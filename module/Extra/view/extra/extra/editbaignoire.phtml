<?php
$title = 'Modifier une baignoire';
$this->headTitle($title);
?>
<h1><?php echo $this->escapeHtml($title); ?></h1>

<?php
$form = $this->form;

$form->setAttribute('action', $this->url('extra', array('action' => 'editbaignoire', 'idChambre' => $this->idChambre, 'idDouche' => $this->idBaignoire)));
$form->prepare();
?>

<div class="asideGestion">
    <?php
    echo $this->form()->openTag($form);
    echo $this->formHidden($form->get('idChambre'));
    echo '<br />';
    echo $this->formHidden($form->get('idBaignoire'));
    echo '<br />';
    echo $this->formRow($form->get('prixBaignoire'));
    echo '<br />';
    ?>
    <input class="btn btn-success" type="submit" name="submit" id="submitbutton" value="Modifier" />
    <a href="<?php echo $this->url('extra', array('idChambre' => $idChambre)); ?>" class="btn btn-success">Retour</a>
    <?php
    echo $this->form()->closeTag();
    ?>
</div>